apiVersion: v1
kind: Pod
metadata:
  labels:
    run: nginx
  name: nginx
spec:
  containers:
  - image: nginx
    name: nginx
    resources: {}
    command: ["ls /notexist"]
  dnsPolicy: ClusterFirst
  restartPolicy: Always
status: {}
